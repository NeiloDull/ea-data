## 2014 EA Survey Analysis

Run the model and analysis:

```
cd ~/path/to/ea-data
R
```

```R
run("2014")
```

-

To create the file from the non-public non-anonymous version, `source("models/2014/ea_id.R")` from within R in the root directory.
